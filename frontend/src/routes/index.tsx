import WordCard from "~/components/WordCard";

/**
 * Home page — displays the "Word of the Day" (currently hardcoded).
 * URL: /
 */
export default function HomePage() {
  return <WordCard word="meaningful" />;
}
